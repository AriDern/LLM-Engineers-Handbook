settings:
  docker:
    parent_image: 794038240560.dkr.ecr.us-east-1.amazonaws.com/zenml-bsxgns:latest
    skip_build: True
  orchestrator.sagemaker:
    synchronous: false

parameters:
  finetuning_type: sft
  num_train_epochs: 3
  per_device_train_batch_size: 2
  learning_rate: 3e-4
  dataset_huggingface_workspace: mlabonne
  is_dummy: false # Change this to 'false' to run the training with the full dataset and epochs.
